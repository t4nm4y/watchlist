import React from 'react'

const Card = (props) => {
  return (
    <div className='card'>
        {props.title}
        {/* add these buttons on hover */}
        {/* edit btn  */}
        {/* delete btn */}
    </div>
  )
}

export default Card